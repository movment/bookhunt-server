type GetBooksResponse {
  ok: Boolean!
  error: String
  books: [Book]
  isFav: Boolean
}
enum SortOptions {
  VIEWS
  PUBDATE
}
type Query {
  GetBooks(bookId: Int, sort: SortOptions): GetBooksResponse!
}
